// Aqui estamos executando uma função de soma onde ela pega os parâmetros
// a e b e retorna a soma entre eles.
// Observe que o primeiro parâmetro da função, sempre é dois.
function sum(a, b) {
  return a + b;
}

sum(2, 1);
sum(2, 2);
sum(2, 3);
sum(2, 4);
sum(2, 5);